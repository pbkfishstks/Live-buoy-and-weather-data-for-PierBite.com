# PIERBITE | Workflow repoint v10 | 2026-08-21T14:40:00Z
# GitHub-side filename MUST stay exactly: .github/workflows/probe (1).yml
# (do not rename the file on GitHub - only replace its contents)
# This version points at: port-washington-node-and-station-probe-2026-08-21-v1.py
# Previous version (v9) pointed at: neshotah-portwashington-coordinates-probe-2026-08-20-v1.py
name: Run diagnostic probe

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run Port Washington probe
        run: python port-washington-node-and-station-probe-2026-08-21-v1.py
