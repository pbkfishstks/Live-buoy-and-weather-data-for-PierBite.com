# PIERBITE | Workflow repoint v6 | 2026-08-20T14:12:28Z
# GitHub-side filename MUST stay exactly: .github/workflows/probe (1).yml
# (do not rename the file on GitHub - only replace its contents)
# This version points at: glos-probe-v3-2026-08-20-v1.py
# Previous version (v5) pointed at: lmhofs-forecast-accuracy-and-mirror-probe-2026-08-19-v2.py
name: Run diagnostic probe

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run GLOS probe v3
        run: python glos-probe-v3-2026-08-20-v1.py
