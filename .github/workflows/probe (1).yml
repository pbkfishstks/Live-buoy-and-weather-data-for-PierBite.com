# PIERBITE | Workflow repoint v11 | 2026-08-21T15:20:00Z
# GitHub-side filename MUST stay exactly: .github/workflows/probe (1).yml
# (do not rename the file on GitHub - only replace its contents)
# This version points at: port-washington-node-and-station-probe-2026-08-21-v2.py
# Previous version (v10) pointed at: port-washington-node-and-station-probe-2026-08-21-v1.py
#   v1 failed its own Stage 3 self-check - its OPeNDAP parser read the
#   response header as data and shifted every node index by six. Fixed
#   in v2 by reusing the proven parser from the 2026-07-25 v6 probe.
name: Run diagnostic probe

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run Port Washington probe
        run: python port-washington-node-and-station-probe-2026-08-21-v2.py
