# PIERBITE | Workflow repoint v13 | 2026-08-21T17:10:00Z
# GitHub-side filename MUST stay exactly: .github/workflows/probe (1).yml
# (do not rename the file on GitHub - only replace its contents)
# This version points at: port-washington-node-and-station-probe-2026-08-21-v4.py
# Previous version (v12) pointed at: ...-v3.py
#   v3 passed all critical checks and CONFIRMED node 17831 on a second
#   run. Its Stage 6 printed an EMPTY variable list and then wrongly
#   concluded station 250 has no water temperature - a false negative
#   caused by a line-oriented .das parser, not by anything at GLOS.
#   v4 copies the repository's proven quote-aware parser verbatim and
#   treats an empty variable list as a fault, never as a finding.
name: Run diagnostic probe

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run Port Washington probe
        run: python port-washington-node-and-station-probe-2026-08-21-v4.py
